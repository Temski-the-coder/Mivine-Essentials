{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": {"distDIR": "dist"}
        }
    ],
    "rewrites": [
        {
            "source": "/(.*)", "destination": "/"
        }
    ]
}